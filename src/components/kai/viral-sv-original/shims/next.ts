/**
 * Stub de `next` — só fornece `Metadata` e `Viewport` types.
 * Páginas do Next standalone exportam `metadata` que no Vite é ignorado.
 */
export type Metadata = Record<string, unknown>;
export type Viewport = Record<string, unknown>;
